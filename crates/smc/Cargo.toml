[package]
name = "smc"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
iscsi-target = { path = "../iscsi-target" }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["sync"] }
tracing = "0.1"
